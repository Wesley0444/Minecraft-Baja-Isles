# Tool binaries — never commit executables to the repo
pack-tools/bin/

# --- Server runtime (step 4) — generated, secret, or huge. Never committed. ---
# The four ops scripts (launch.bat, update.bat, admin_setup.bat, setup-tasks.ps1,
# backup.ps1) ARE committed — they hold no secrets (RCON password is read from
# server.properties at runtime, and server.properties is ignored below).
/libraries/
/mods/
/world/
/world*/
/logs/
/crash-reports/
/config/
/defaultconfigs/
/eula.txt
/server.properties
/usercache.json
/usernamecache.json
/banned-ips.json
/banned-players.json
/ops.json
/whitelist.json
/maintenance.lock
/sparsestructures/
/.mixin.out/
/dynamic-data-pack-cache/
/run.bat
/run.sh
/user_jvm_args.txt
/*.jar
/*.log
/packwiz.json

# Secrets — NEVER commit. A bot token landed in the repo once (2026-08-30, revoked).
discord-bot-token.txt
*token*
*.secret

# Distributed instance zips — served to friends by the cards site's download
# button, never through this public repo (they may carry servers.dat = the
# server address).
pack-tools/instance/*.zip

# Server-side mods we build ourselves (mods-src/): source is committed, build output is not —
# the jar ships as a GitHub Release asset + packwiz stub, same as the AC magnetism patch.
mods-src/*/build/
